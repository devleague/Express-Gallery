<!DOCTYPE html>
html(lang="en")
  head
    meta(charset="UTF-8")
    title Express Gallery
  body
    h1 Create new gallery #{name}
    form(method="POST", action="/gallery", enctype="x-www-form-urlencoded")
      p Author's Name
      input(type="text", name="author")
      p URL
      input(type="text", name="link")
      p Description
      input(type="text", name="description")
      p
      button(type="submit") Submit