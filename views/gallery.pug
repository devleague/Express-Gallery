<!DOCTYPE html>
html(lang="en")
  head
    meta(charset="UTF-8")
    title Express Gallery
    link(rel="stylesheet" type="text/css" href="/styles/styles.css")
  body
    div
      each val in picture
        img(src=val.url id="picture")
        li= val.author
        li= val.description
