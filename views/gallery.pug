<!DOCTYPE html>
html(lang='en')
  head
    meta(charset='UTF-8')
    title Express Gallery
  body
    img(src=gallery.link)
    p Uploaded by: #{gallery.author}
    p Description: #{gallery.description}
    p
      button(type="submit" onclick="redirect()") Update
script.
  function redirect() {
    window.location.href = 'update/#{gallery.id}'}
