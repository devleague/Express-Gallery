<!DOCTYPE html>
html(lang="en")
head
  meta(charset="UTF-8")
  title Document
body
  form(method="POST" action="/gallery/" + gallery.id enctype="application/x-www/form-urlencoded")
    input(type="hidden" name="_method" value="PUT")
    p Author
    input(type="text" name="author" value=gallery.author)
    p Link
    input(type="text" name="link" value=gallery.link)
    p Description
    input(type="text" name="description" value=gallery.description)
    p
      button(type="submit") Update Gallery
