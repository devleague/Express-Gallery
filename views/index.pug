<!DOCTYPE html>
html(lang='en')
head
  meta(charset='UTF-8')
  title Express Gallery
body
  h1 Welcome to the gallery!
  if new Date().getHours() >= 18
    h2 Good evening :)
  h2 The time is currently #{new Date()}
  h3 You are currently visitor #{visitorCount}!
  div
    button(onclick="redirect()") Post a photo
  each gallery in galleries
    img(src=gallery.link onclick="select()")
script.
  function redirect() {
    window.location = "gallery/new"}
  function select() {
    window.location = "gallery/#{gallery.id}"}